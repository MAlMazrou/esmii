services:
  caddy:
    networks:
      - production-monitoring-edge
    volumes:
      - ./caddy/sites-enabled/production-dashboard.caddy:/etc/caddy/sites-enabled/production-dashboard.caddy:ro
    depends_on:
      production-dashboard:
        condition: service_healthy
